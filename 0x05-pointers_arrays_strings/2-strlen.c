#include "main.h"
#include <string.h>

/**
 * _strlen - update the value
 * the pointer points to to 98
 *
 *  @s: pointer to he variable  
 *  Return:  lenth
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
