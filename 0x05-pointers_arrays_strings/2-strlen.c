#include "main.h"
#include <string.h>

/**
 * _strlen - update the value
 * the pointer points to to 98
 *
 *  @a: pointer to he variable
 *  
 *  return - lenth
 */

int _strlen(char *s)
{
	int len;

	len = strlen(*s);

	return(len);
}
