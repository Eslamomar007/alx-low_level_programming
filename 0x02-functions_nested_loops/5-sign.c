#include <stdio.h>
#include "main.h"
/**
*      _isalpha - Prints "Programming is ing new line with puts"
*      @c: The character to print
*      Return: Always1
*/

int _isalpha(int c)
{
	if (n > 0 )
	{
		_putchar('+');
		_putchar(44);
		_putchar('\n');
		return (1);
	}
  	if (n < 0 )
        {
	       	_putchar('-');
		_putchar(44);
		_putchar('\n');
		return ("-1");
	}

	putchar(48);
	putchar(44);
	putchar('\n');
	return (0);
}
