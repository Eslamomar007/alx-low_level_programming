#include "main.h"
#include <ctype.h>

/**
 * _isupper - functiois upper
 * @c: char varialnladsf 
 * Return: 1 for true 0 for false
 */

int _isupper(int c)
{
	if (isupper(c))
		return (1);
	else
		return (0);
}
