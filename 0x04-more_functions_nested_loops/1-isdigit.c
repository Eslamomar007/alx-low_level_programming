#include "main.h"
#include <ctype.h>

/**
 * _isdigit - functionsdf
 *
 * Description: function asdffasdthat can check uppercase char
 *
 * @c: input paramasdf
 *
 * Return: 1 for true 0 for false
 */

int _isdigit(int c)
{
	if (isdigit(c))
		return (1);
	else
		return (0);
}
